import * as React from "react";
import * as ReactDOM from "react-dom";
import Router from "./src/scripts/components/router";
ReactDOM.render(
  <Router></Router>
  ,
  document.getElementById("root")
);